namespace SmartDots.Helpers
{
    public enum EditorModeEnum { None, DrawLine, MakingLine, DrawDot, SelectLine, DragImage, Delete, DrawScale, Measure, MakingMeasure }
    public enum DtoAuthenticationMethod { Windows, Basic, Token}
}
