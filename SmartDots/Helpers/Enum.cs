namespace SmartDots.Helpers
{
    public enum EditorModeEnum { None, DrawLine, MakingLine, DrawDot, DrawCircle, MakingCircle, SelectLine, DragImage, Delete, DrawScale, Measure, MakingMeasure, Move }
    public enum DtoAuthenticationMethod { Windows, Basic, Token}
}
